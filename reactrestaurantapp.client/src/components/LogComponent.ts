//import * as signalR "@microsoft/signalr";

//const connection = new signalR.HubConnectionBuilder("").withUrl.("").build(); 

//connection.on("ReceiveLog", (message) => {
//    console.log(message);
//})

//connection.start().catch(err => console.erro(err));